{
    "name": "Host PC",
    "image": "ghcr.io/eccentricorange/int_brain_host:amd64-v1.0.1",

    "workspaceMount": "source=${localWorkspaceFolder},target=/home/ubuntu/int_brain_ws/src,type=bind",
    "workspaceFolder": "/home/ubuntu/int_brain_ws",

    "privileged": true,

    "containerEnv": {
        "DISPLAY": "${localEnv:DISPLAY}"
    },

    "remoteUser": "ubuntu",

    "runArgs": [
        "--net=host",
        "--ipc=host",
        "--volume=/dev:/dev"
    ],
    
    "initializeCommand": "git submodule update --init --recursive && xhost +local:root",
    "postCreateCommand": "$WORKSPACE/src/config/scripts/post_create.sh"
}